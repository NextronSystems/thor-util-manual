.. role:: raw-html-m2r(raw)
   :format: html

Install Packages (install)
==========================

The “install” feature is only used to install previously downloaded
packages using the “download” feature and often used on systems without
Internet connection.
